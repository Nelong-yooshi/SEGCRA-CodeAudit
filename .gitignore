# macOS
.DS_Store

# Python
__pycache__/
*.pyc
.venv/
# 例如產生標準答案用的 .venv-dbt
.venv-*/
.pytest_cache/

# 執行產物
review_output/
**/eval/_output/
# baseline 跑批的輸出。不追蹤是刻意的,而且有第二個理由:preflight 用
# `git status --porcelain` 判斷程式碼是不是 dirty,而 dirty 的 baseline 會被標成
# 「不可當共用基準」。跑批把輸出寫進 repo 裡,不 ignore 的話**第二輪起每一份
# baseline 都會因為前一輪的輸出而被自己標成 dirty**——工具自己弄髒自己。
**/eval/baselines/

# dbt 對照專案(若直接在該目錄手動執行 dbt 會產生這些)
**/dbt_reference/target/
**/dbt_reference/logs/
**/dbt_reference/dbt_packages/
**/dbt_impact_reference/target/
**/dbt_impact_reference/logs/
**/dbt_impact_reference/dbt_packages/
*.duckdb
*.duckdb.wal

# 憑證 / 環境變數(絕不提交)
config/gitlab.env
*.env
!*.env.example
