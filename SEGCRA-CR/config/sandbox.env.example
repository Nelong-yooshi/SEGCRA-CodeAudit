# 執行驗證沙盒(SQL Server)的連線設定。複製成 config/sandbox.env 後填入密碼。
# config/*.env 已被 .gitignore 排除,不要 commit。
#
# 密碼規則(SQL Server 要求):至少 8 碼,且包含大寫、小寫、數字、符號其中三類。
SANDBOX_MSSQL_PASSWORD=
SANDBOX_MSSQL_HOST=127.0.0.1
SANDBOX_MSSQL_PORT=1433
SANDBOX_MSSQL_USER=sa
