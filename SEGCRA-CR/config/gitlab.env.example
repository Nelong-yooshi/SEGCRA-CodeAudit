# 複製成 config/gitlab.env(或直接 export 同名環境變數)後填入實際值。
# 設定齊全後 gitlab 工具走 real 模式;缺任一則走 mock 模式(fixtures/)。
# token 請自行設定,勿提交至版本控制(config/gitlab.env 已列入 .gitignore)。
GITLAB_URL=http://localhost:8929
GITLAB_TOKEN=<your-token>
GITLAB_PROJECT=<專案 id>
